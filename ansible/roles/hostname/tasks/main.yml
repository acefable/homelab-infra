---
- name: Set hostname
  ansible.builtin.hostname:
    name: "{{ hostname_name }}"

- name: Update /etc/hosts with canonical hostname
  ansible.builtin.lineinfile:
    path: /etc/hosts
    regexp: '^127\.0\.1\.1\s+'
    line: "127.0.1.1 {{ hostname_name }}"
    state: present
