---
- name: Wait for K3s readiness
  ansible.builtin.command:
    cmd: "kubectl wait --for=condition=Ready nodes --all --timeout=150s"
  register: k3s_ready
  retries: 5
  delay: 5
  until: k3s_ready.rc == 0
  changed_when: false
